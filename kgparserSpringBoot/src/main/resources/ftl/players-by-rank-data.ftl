if (typeof STATS_DATA === 'undefined') {
    var STATS_DATA = {};
}

/* 2D-array. In each line, 0-th element is ratingLevel, 1-st element is totalRacesCount */
STATS_DATA.rankToTotalRacesCount = ${rankToTotalRacesCount};
